<!DOCTYPE html>
<html>
  <head>
    <title>AdoptDontShop</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <nav>
      <p>
      <%= link_to 'All Pets', '/pets', id: "nav-link" %>
      <%= link_to 'All Shelters', '/shelters', id: "nav-link" %>
      <%= link_to 'Favorite Pets:', '/favorites', id: "nav-link" %> <%= favorites_list.total_count %>
      </p>
    </nav>

    <% flash.each do |type, message| %>
    <section class = "flash-<%=type%>">
      <p class = "flash-<%=type%>"><%= message %></p>
    </section>
    <% end  %>

    <%= yield %>

  </body>
</html>
